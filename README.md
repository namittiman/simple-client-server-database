To compile -:
	Go to the location of folder in terminal and write make and press enter.

To run -:
	To start server,write ./server followed by port number and press enter in the terminal.
	To start a client,open a new terminal and write ./client followed by ip address and port number of server.
	Now you can execute various queries.